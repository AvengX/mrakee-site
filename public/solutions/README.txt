Drop the generated solution images here as 01.jpg ... 17.jpg,
in the same order as the SOLUTIONS array in src/components/BrandPage.jsx.

Any number present is used; any number missing falls back to a still from
the film automatically, so partial deliveries are fine.

Prompts: film-src/SOLUTION_PROMPTS.md
